<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Labels</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }

    
    .label {
      width: 57.4mm;
      height: 40mm;
      text-align: center;
      border: 1px solid #000;
      font-size: 10px;
    }

    .logo {
      height: 30px;
      width: 30px;
      display: inline-block;
      object-fit: contain;
    }

    .order-number,
    .company-name {
      font-weight: bold;
      font-size: 12px;
    }

    .details {
      font-size: 10px;
      line-height: 1.1;
    }

    .remarks {
      font-size: 10px;
      font-style: italic;
    }
  </style>
</head>

<body>
  <div class="label-container">
    <% items.forEach((item, index)=> { %>
      <div class="label">
        <img src="<%= logoUrl %>" alt="Company Logo" class="logo">
        <div class="company-name">SIKKA CLEANERS</div>
        <div class="order-number">Order #: <%= orderNumber %>
        </div>
        <div class="details">Date: <%= date %>
        </div>
        <div class="details">Customer: <%= customerName %>
        </div>
        <div class="details">Item: <%= index + 1 %>/<%= items.length %>
        </div>
        <div class="details">Service: <%= item.serviceName %>
        </div>
        <% if (item.remarks) { %>
          <div class="remarks">Remarks: <%= item.remarks %>
          </div>
          <% } %>
      </div>
      <% }); %>
  </div>
</body>

</html>