<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Labels</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .label {
      width: 50mm;
      height: 35.9mm;
      text-align: center;
      border: 1px solid #000;
      font-size: 10px;
      margin: 0;
      padding: 0;
    }

    .logo {
      height: 30px;
      width: 30px;
      display: inline-block;
      object-fit: contain;
    }

    .order-number,
    .company-name {
      font-weight: bold;
      font-size: 12px;
    }

    .details {
      font-size: 10px;
      line-height: 1.1;
    }

    .remarks {
      font-size: 10px;
    }
  </style>
</head>

<body>
  <div class="label-container" style="margin: 0; padding: 0 ;">
    <% items.forEach((item, index)=> { %>
      <div class="label">
        <img src="<%= logoUrl %>" alt="Company Logo" class="logo">
        <div class="company-name">SIKKA CLEANERS</div>
        <% if (date) { %>
          <div class="date">Date : <%= date %>
          </div>
          <% } %>
        <div class="order-number">Order #: <%= orderNumber %>
        </div>
       
        <div class="details" style="font-size: 12px;  font-weight: bold;">Customer: <%= customerName %>.
        </div>
       
        <div class="details">Item: <%= productName %> (<%= index + 1 %>/<%= itemsLegth %>) ,  QTY : <%= itemsQty %>
        </div>
        <div class="details">Service: <%= serviceName %>
        </div>
        <% if (remarks) { %>
          <div class="remarks">Remarks: <%= remarks %>
          </div>
          <% } %>
      </div>
      <% }); %>
  </div>
</body>

</html>