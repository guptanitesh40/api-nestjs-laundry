<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Labels</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .label {
      width: 57.4mm;
      height: 40mm;
      min-height: 40mm;
      text-align: center;
      border: 1px solid #000;
      font-size: 10px;
      margin: 0;
      padding: 0;
    }

    .logo {
      height: 30px;
      width: 30px;
      display: inline-block;
      object-fit: contain;
    }

    .order-number,
    .company-name {
      font-weight: bold;
      font-size: 12px;
    }

    .details {
      font-size: 10px;
      line-height: 1.1;
    }

    .remarks {
      font-size: 10px;
      font-style: italic;
    }
  </style>
</head>

<body>
  <div class="label-container" style="margin: 0; padding: 0 ;">
    <% items.forEach((item, index)=> { %>
      <div class="label">
        <img src="<%= logoUrl %>" alt="Company Logo" class="logo">
        <div class="company-name">SIKKA CLEANERS</div>
        <div class="order-number">Order #: <%= orderNumber %>
        </div>
        <div class="details">Date: <%= date %>
        </div>
        <div class="details">Customer: <%= customerName %>
        </div>
        <div class="details">Item: <%= index + 1 %>/<%= items.length %>
        </div>
        <div class="details">Service: <%= item.serviceName %>
        </div>
        <div class="details">Product: <%= item.productName %>
        </div>
        <% if (item.remarks) { %>
          <div class="remarks">Remarks: <%= item.remarks %>
          </div>
          <% } %>
      </div>
      <% }); %>
  </div>
</body>

</html>